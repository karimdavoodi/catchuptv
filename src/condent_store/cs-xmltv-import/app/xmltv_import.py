#!/usr/bin/python3
import os
import time
import json

import util

gb_env = util.get_env([
        'CS_DB_INFO_URL',
        'CS_DB_INFO_DB',
        'CHANNEL_NAME',
        'XMLTV_URL'
        ])


if __name__ == '__main__':
    while True:
        util.error("TODO:... ")
        time.sleep(30)
    exit(0)
