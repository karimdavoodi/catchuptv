#!/usr/bin/python3
import os
import time
import json

import util

gb_env = util.get_env([
        'CS_DB_SEG_URL',
        'CS_DB_SEG_DB',
        'FILE_NAME',
        'SUBTITLE_LANGUAGE',
        'SUBTITLE_URL'
        ])

if __name__ == '__main__':
    util.error("TODO:... ")
    exit(0)
